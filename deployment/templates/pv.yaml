apiVersion: v1
kind: PersistentVolume
metadata:
  name: esef-data-pv
  labels:
    app: esef-data-fetcher
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 10.44.0.6
    path: /srv/nfs/gmr
  mountOptions:
    - hard
    - nfsvers=4.1
